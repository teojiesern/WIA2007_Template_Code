package com.example.practical_3;

import android.content.Context;
import android.content.SharedPreferences;

public class SharedPreferenceTemplate {
    // this is the read for shared preference
    // SharedPreferences preferences =
    // getActivity().getSharedPreferences(preference_constant.pUserInfo,
    // Context.MODE_PRIVATE);
    // String userId = preferences.getString(preference_constant.pUserId, "");

    // this is the write
    // SharedPreferences preferences =
    // getActivity().getSharedPreferences(preference_constant.pUserInfo,
    // Context.MODE_PRIVATE);
    // SharedPreferences.Editor spEditor = preferences.edit();
    // spEditor.putBoolean(preference_constant.pIsLogin,true);
    // spEditor.putString(preference_constant.pUserId,userId);
    // spEditor.commit();
}
